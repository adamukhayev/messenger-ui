import React from 'react'

const Account = () => {

  return(
      <div
      style={{margin: 250}}>
        <div>
          <button style={{ backgroundColor:'#4B0082', color: "white", width: 900, height: 40}}>способ проверки</button>
        </div>
        <div>
        <h1 style={{fontWeight: "bold", fontSize: 40, color: '#F0FFF0'}}>Выбирети способ для проверки подписи</h1>
        </div>
      </div>
  )
}

export default Account